[
  {
    "name": "get_weather",
    "description": "Obtenir la météo actuelle d'une ville donnée",
    "parameters": {
      "type": "object",
      "properties": {
        "city": {
          "type": "string",
          "description": "Nom de la ville (ex: Paris, Marseille, etc.)"
        }
      },
      "required": ["city"]
    }
  },
  {
    "name": "get_coordinates_openmeteo",
    "description": "Obtenir les coordonnées GPS d'une ville donnée",
    "parameters": {
      "type": "object",
      "properties": {
        "city": {
          "type": "string",
          "description": "Nom de la ville (ex: Paris, Marseille, etc.)"
        }
      },
      "required": ["city"]
    }
  }
]